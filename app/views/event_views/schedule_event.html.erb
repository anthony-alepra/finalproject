<h1>Schedule an Event!</h1>
<h3>We've accepted your application. Now it's time to schedule a playtest!</h3>

<%= form_with model: @event do |f| %>
    <%= f.label :date %>
    <%= f.date_field :date %><br>

    <%= f.label :time %>
    <%= f.time_field :time %><br>

    <%= f.submit %>
<% end %>